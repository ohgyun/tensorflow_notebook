Tensorflow study notebook

## Installation
Tensorflow notebook runs with docker images. If you haven't installed Docker, you can install it at [Docker](docker.io) homepage.

## Run notebook
```
$ ./run_notebook.sh
```
